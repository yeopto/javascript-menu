const COUNT = {
  zero: 0,
  one: 1
};

const SPLIT_CHARACTER = {
  comma: ',',
  divisionLine: ' | ',
};

const CATEGORY_AND_MENU_NUM = {
  startNum: 1,
  endNum: 5,
  max: 5,
  canOverlapNum: 2,
  DayCount: 5
};

const VALIDATION_NUM = {
  minCoachNameLength: 2,
  maxCoachNameLength: 4,
  minTogetherEatPeopleCount: 2,
  maxTogetherEatPeopleCount: 5,
  maxNotEatMenuCount: 2,
}

module.exports = {
  COUNT,
  SPLIT_CHARACTER,
  CATEGORY_AND_MENU_NUM,
  VALIDATION_NUM,
};
